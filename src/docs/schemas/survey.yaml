SurveyReportResponse:
  type: object
  properties:
    id:
      $ref: '#/components/schemas/UUID'
    job_id:
      $ref: '#/components/schemas/UUID'
    surveyor_id:
      $ref: '#/components/schemas/UUID'
    survey_status:
      type: string
      enum: [NOT_STARTED, STARTED, CHECKLIST_SUBMITTED, PROOF_UPLOADED, SUBMITTED, REWORK_REQUIRED, FINALIZED]
    submission_count:
      type: integer
    declared_by:
      $ref: '#/components/schemas/UUID'
    declared_at:
      type: string
      format: date-time
    start_latitude:
      type: number
    start_longitude:
      type: number
    submit_latitude:
      type: number
    submit_longitude:
      type: number
    attendance_photo_url:
      type: string
      format: uri
    evidence_proof_url:
      type: string
      format: uri
    signature_url:
      type: string
      format: uri
    declaration_hash:
      type: string
    survey_statement:
      type: string
    survey_statement_status:
      type: string
      enum: [NOT_PREPARED, DRAFTED, ISSUED]
    survey_statement_pdf_url:
      type: string
      format: uri
    signed_checklist_files:
      type: array
      items: { type: string }
    started_at:
      type: string
      format: date-time
    submitted_at:
      type: string
      format: date-time
    finalized_at:
      type: string
      format: date-time
  example:
    id: "01933c5e-7f2a-7a00-8000-1a2b3c4d5e6f"
    job_id: "01933c5e-7f2a-7a00-8000-1a2b3c4d5e6f"
    survey_status: "SUBMITTED"
    submission_count: 1
    submit_latitude: 1.3521
    submit_longitude: 103.8198
    survey_statement: "Survey completed successfully."
    declared_by: "01933c5e-7f2a-7a00-8000-1a2b3c4d5e6f"
    declared_at: "2026-02-25T12:00:00Z"
    declaration_hash: "a591a6d40bf420404a011733cfb7b190d62c65bf0bcda32b57b277d9ad9f146e"
    started_at: "2026-02-25T10:00:00Z"
    submitted_at: "2026-02-25T12:00:00Z"

SurveyTimelineResponse:
  type: object
  properties:
    job_id:
      $ref: '#/components/schemas/UUID'
    gps_trace:
      type: array
      items:
        type: object
        properties:
          latitude: { type: number }
          longitude: { type: number }
          timestamp: { type: string, format: date-time }
    survey_details:
      $ref: '#/components/schemas/SurveyReportResponse'

SurveyListResponse:
  type: object
  properties:
    id:
      $ref: '#/components/schemas/UUID'
    job_id:
      $ref: '#/components/schemas/UUID'
    surveyor_id:
      $ref: '#/components/schemas/UUID'
    survey_status:
      type: string
      enum: [NOT_STARTED, STARTED, CHECKLIST_SUBMITTED, PROOF_UPLOADED, SUBMITTED, REWORK_REQUIRED, FINALIZED]
    submission_count:
      type: integer
    started_at:
      type: string
      format: date-time
    submitted_at:
      type: string
      format: date-time
    finalized_at:
      type: string
      format: date-time
    survey_statement_status:
      type: string
      enum: [NOT_PREPARED, DRAFTED, ISSUED]
    survey_statement_pdf_url:
      type: string
      format: uri
    signed_checklist_files:
      type: array
      items: { type: string }
    JobRequest:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/UUID'
        job_status:
          type: string
        Vessel:
          type: object
          properties:
            vessel_name: { type: string }
            imo_number: { type: string }
    User:
      type: object
      properties:
        name: { type: string }
        email: { type: string }
